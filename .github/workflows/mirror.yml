name: Mirror to Public

on:
  push:
    branches: [ main, dev ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mirror:
    if: ${{ github.repository == 'expolicores/expolicores-app' }}
    runs-on: ubuntu-latest
    concurrency:
      group: mirror-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name  "expolicores-bot"
          git config user.email "bot@users.noreply.github.com"

      - name: Add mirror remote
        env:
          MIRROR_TOKEN: ${{ secrets.MIRROR_TOKEN }}
        run: |
          git remote add mirror https://x-access-token:${MIRROR_TOKEN}@github.com/expolicores/expolicores-app-public.git
          git remote -v

      - name: Push current branch to mirror
        env:
          BRANCH: ${{ github.ref_name }}
          MIRROR_TOKEN: ${{ secrets.MIRROR_TOKEN }}
        run: |
          git push --force-with-lease https://x-access-token:${MIRROR_TOKEN}@github.com/expolicores/expolicores-app-public.git "$BRANCH:$BRANCH"

      - name: Push tags (optional)
        env:
          MIRROR_TOKEN: ${{ secrets.MIRROR_TOKEN }}
        run: |
          git push https://x-access-token:${MIRROR_TOKEN}@github.com/expolicores/expolicores-app-public.git --tags
